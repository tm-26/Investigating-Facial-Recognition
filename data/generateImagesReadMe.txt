generateImages.py is used to generate the Olivetti dataset from the face_data.cvs file.
If the Olivetti dataset is present, this does not need to be ran

To run generateImages.py make sure the following are installed:
python >= 3.8.5
opencv (pip install opencv-python)
numpy (pip install numpy)
pandas (pip install pandas)
